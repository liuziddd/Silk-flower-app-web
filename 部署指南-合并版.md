# 部署指南：合并版前后端项目(Vercel部署)

本指南适用于已经合并前后端的花小绢绢花项目，通过Vercel部署一体化应用。

## 1. 项目结构说明

合并后的项目结构：
- `src/` - 前端Vue应用源码
- `api/` - 后端Express API服务
- `public/` - 前端静态资源和后端静态资源
- `private/` - 后端私有资源(3D模型等)
- `dist/` - 构建输出目录(部署时自动生成)

## 2. 准备工作

### 环境变量设置

部署前需要在Vercel中配置以下环境变量：
1. `ARK_API_KEY` - 火山方舟API密钥(用于聊天和图像生成)
2. `ENDPOINT_ID` - 火山方舟聊天模型接入点
3. `FRONTEND_URL` - 前端应用的URL，通常是Vercel分配的域名
4. `VERCEL` - Vercel会自动设置为`1`，用于识别部署环境
5. `NODE_ENV` - Vercel会自动设置为`production`

### 3D模型文件说明

由于Vercel对文件大小有限制(最大限制为50MB)，原项目中的3D模型文件(red.fbx: 118MB, pink.fbx: 135MB)无法直接部署。
合并版项目做了以下适配：
1. 提供了轻量级的`model-service-vercel.js`，在Vercel环境中自动启用
2. 不上传实际的大型模型文件，仅提供模型信息
3. 提供了`model.json`描述文件说明模型情况

**注意：实际生产环境中应该：**
- 将大型3D模型文件存储在CDN或对象存储服务(如阿里OSS、七牛云等)
- 修改`model-service.js`从CDN加载模型

## 3. 部署到Vercel步骤

1. 在GitHub上创建仓库并上传项目代码：
   ```bash
   git init
   git add .
   git commit -m "合并前后端项目"
   git remote add origin https://github.com/你的用户名/silk-flower-web.git
   git push -u origin main
   ```

2. 在Vercel中导入项目:
   - 访问 [vercel.com](https://vercel.com) 并登录
   - 点击"Add New" → "Project"
   - 导入GitHub仓库
   - 配置环境变量(点击"Environment Variables"添加上述变量)
   - 点击"Deploy"开始部署

3. 部署完成后，Vercel会提供一个URL，访问它来验证部署是否成功

## 4. 本地开发说明

1. 本地开发需要创建`.env.development`文件(不要提交到Git):
   ```
   VITE_API_URL=http://localhost:3000
   ARK_API_KEY=你的火山方舟API密钥
   ENDPOINT_ID=你的模型接入点ID
   ```

2. 开发模式启动前端:
   ```bash
   npm run dev
   ```

3. 开发模式启动后端(新终端):
   ```bash
   npm run dev:server
   ```

## 5. 项目限制与说明

1. Vercel部署版本中，3D模型服务只提供模型信息，不提供实际模型文件
2. 火山方舟API需要有效的API密钥，图像生成和聊天功能依赖此API
3. 本地开发时需要有3D模型文件在`private/3d/`目录，但不需要上传到Git
4. 线上环境推荐使用CDN存储3D模型文件，并相应修改`model-service.js`

## 6. 故障排查

1. **API 404错误**：检查vercel.json路由配置是否正确
2. **3D模型加载失败**：Vercel环境不提供大型模型文件，需通过修改model-service-vercel.js适配CDN
3. **环境变量问题**：检查Vercel中是否正确设置了所有必要的环境变量
4. **聊天API错误**：检查火山方舟API密钥和接入点ID是否有效

## 7. 进一步优化

1. 添加缓存策略提高API响应速度
2. 使用Vercel Edge Functions优化全球访问
3. 将大型静态资源分离到专门的对象存储或CDN
4. 监控API使用情况，避免超出免费额度 